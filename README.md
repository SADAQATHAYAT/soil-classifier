# soil-classifier
“Classifies soil samples using USCS rules, computes D-values and coefficients, generates grain-size and plasticity charts, and predicts soil types with a machine learning model. Provides an interactive GUI for input, visualization, dataset management, and result export.”
